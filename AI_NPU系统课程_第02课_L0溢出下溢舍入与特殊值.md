# 第 02 课｜L0.3：overflow、underflow、舍入误差与特殊值

## 一、学习目标

完成本课后，你应能：

1. 区分 overflow、underflow、subnormal、`inf` 与 `NaN`；
2. 说明 FP16 与 BF16 为什么在大数范围上表现不同；
3. 理解“尾数位有限”为什么会造成舍入误差；
4. 在 NPU 精度问题中，用 `max_abs`、`inf/NaN` 比例与单变量实验定位数值异常；
5. 复习 FP16 的 `E=e-bias` 和“指数位管范围、尾数位管精度”。

---

## 二、先做一次间隔复习

FP16 的存储指数为 `e=13`，bias 为 15。真实指数是多少？

```text
E = ______
```

**我的回答：** `E=-2`。

**批改：正确。**

```text
E = e - bias = 13 - 15 = -2
```

---

## 三、什么是 overflow

当计算结果的绝对值超过格式可表示的最大有限值时，就发生 **overflow（上溢）**。常见结果是 `+inf` 或 `-inf`。

```text
FP16 最大有限值约为 65,504

70,000 存成 FP16
→ 超过上限
→ 通常得到 +inf
```

BF16 的指数位有 8 bit，范围接近 FP32，因此 `70,000` 通常仍能表示为有限值；但这不代表它更精确，只代表它更不容易因“数太大”溢出。

### NPU 场景

若 FP32 模型正常、FP16 模型某层先出现 `inf`，优先检查：

1. 该层 `max_abs` 是否超过 65,504；
2. `exp`、softmax、归一化、除法和累加等敏感算子；
3. 将该层临时保留为 BF16 或 FP32 后，异常是否消失。

---

## 四、什么是 underflow

当一个非零的极小数小到格式无法表示时，就发生 **underflow（下溢）**。数值可能变成更小精度的 subnormal，或直接变成 0。

```text
FP16 最小正常数 = 2⁻¹⁴
```

**答疑：为什么最小正常数是 `2⁻¹⁴`？** FP16 有 5 bit 指数、`bias=15`。正常规格化数的指数栏不能全为 0 或全为 1，只能取 `1～30`。最小的正常指数编码是 `e=1`，所以：

```text
E = e - bias = 1 - 15 = -14
最小正常有效数字 = 1.0
最小正常数 = 1.0 × 2⁻¹⁴ = 2⁻¹⁴
```

`e=0` 被保留给 0 和 subnormal，因此正常数不能使用 `E=-15`。
指数栏全为 0 时，FP16 可用 subnormal 表示一段更靠近 0 的极小区间；理论上最小正 subnormal 是 `2⁻²⁴`。但一些硬件或编译选项会将 subnormal 直接刷新为 0，称为 FTZ（flush-to-zero）。

### 直观理解

- overflow：数太大，超过尺子的上端；
- underflow：数太小，落到尺子的下端以下；
- subnormal：在 0 附近额外提供的一小段更细但精度较低的刻度。

---

## 五、为什么会有舍入误差

尾数位有限，意味着不是每个实数都能精确写入浮点格式。存储时只能选择邻近的一个可表示值，这个过程叫 **rounding（舍入）**。

```text
FP16 有 10 bit 尾数
→ 同一数量级中可表示的刻度数量有限
→ 数值越大，相邻刻度的绝对间隔通常越大
```

因此，小差异在大数附近可能被舍掉。例如两个很接近的大激活值，转换为 FP16 后可能落到同一个可表示值。BF16 只有 7 bit 尾数，刻度更粗，舍入误差通常更明显。

---

## 六、特殊指数编码

以 FP16 为例：

| 指数位 | 含义 |
|---|---|
| 全 0，尾数全 0 | `+0` 或 `-0` |
| 全 0，尾数非 0 | subnormal |
| 1～30 | 正常规格化数 |
| 全 1，尾数全 0 | `+inf` 或 `-inf` |
| 全 1，尾数非 0 | `NaN` |

`NaN` 的特点是：它会向后续计算传播。一次除零、`inf-inf` 或对负数做非法开方，都可能产生 `NaN`。

---

## 七、带做例题

### 例题 1：FP16 还是 BF16

某层 activation 的 `max_abs=90,000`。

```text
90,000 > 65,504
```

因此该层使用 FP16 有 overflow 风险。优先将该层保留为 BF16 或 FP32，并用单变量实验检查异常是否消失。

### 例题 2：如何找第一个坏 Tensor

当最终输出出现 `NaN` 时，不要直接看最后一层。应沿模型执行顺序记录每个关键 Tensor 的：

```text
min、max、mean、max_abs、inf_count、nan_count
```

第一个从有限值变为 `inf/NaN` 的 Tensor 是 **First Bad Tensor**。围绕它只改一个变量，例如仅将该层改为 BF16，再比较异常是否消失。

---

## 八、本课练习

### 第 1 题｜bias 复习

FP16 中 `e=14`，真实指数 `E` 是多少？

**我的回答：**
答案：-1

**批改：正确。** `E=14-15=-1`。

### 第 2 题｜overflow 判断

`70,000` 转成 FP16 时最可能发生什么？为什么转成 BF16 通常不会遇到同样的范围问题？

**我的回答：**
答案：向上溢出，因为bf16数值范围更大，指数位为8

**批改：核心正确。** 更准确地说：`70,000` 超过 FP16 最大有限值约 `65,504`，发生 **overflow（上溢）**，通常变为 `+inf`。BF16 有 8 bit 指数位、`bias=127`，指数范围接近 FP32，因此通常可将 `70,000` 保存为有限值；这是范围更大，不代表尾数精度更高。

### 第 3 题｜underflow 判断

用一句话解释 underflow 与 overflow 的区别。

**我的回答：**

数值太小表示不了是 underflow；数值太大表示不了是 overflow。

**批改：正确。**

### 答疑：FP16、BF16、FP32 的范围怎样计算？

对一个有 `k` 个指数位、`p` 个尾数位、bias 为 `B` 的二进制浮点格式：

```text
最小正常数 = 2^(1-B)
最大有限数 = (2 - 2^(-p)) × 2^((2^k - 2) - B)
最小正 subnormal = 2^(1-B-p)
```

| 格式 | 指数位 k | 尾数位 p | bias | 最大有限值约为 | 最小正常数 |
|---|---:|---:|---:|---:|---:|
| FP16 | 5 | 10 | 15 | 65,504 | `2⁻¹⁴` |
| BF16 | 8 | 7 | 127 | `3.39×10³⁸` | `2⁻¹²⁶` |
| FP32 | 8 | 23 | 127 | `3.40×10³⁸` | `2⁻¹²⁶` |

BF16 与 FP32 的指数位和 bias 相同，所以范围接近；差别在 BF16 尾数只有 7 bit，舍入精度较低。


### 第 4 题｜特殊值

FP16 的指数位全为 1、尾数全为 0 时表示什么？若尾数非 0 呢？

**我的回答：**

尾数为 0 代表整数，非零代表小数。

**批改：错误。** 这里的判断依赖于“指数位全为 1”这个特殊前提：

```text
指数全为 1，尾数全为 0 → +inf 或 -inf（由符号位决定）
指数全为 1，尾数非 0 → NaN（不是一个普通数）
```

普通规格化数中，尾数为 0 只表示有效数字是 `1.0`，并不等于“它一定是整数”；还要看真实指数。例如 `1.0×2⁻¹=0.5`，尾数为 0 但数值不是整数。


### 第 5 题｜Debug 思路

FP16 模型最终输出出现 `NaN`。请写出“First Bad Tensor”和一个单变量实验。

**我的回答：**

找到第一个出现 NaN 的 Tensor；不知道什么是单变量实验。

**批改：方向正确，定义补全。** First Bad Tensor 是按模型执行顺序观察统计信息后，第一个从“有限值”变为 `inf` 或 `NaN` 的 Tensor；它可能先出现 `inf`，随后下游才出现 `NaN`，所以不能只找最终的 NaN。

**答疑：什么是单变量实验？** 每次实验只改变一个因素，其他条件完全不变，从而判断该因素是否导致问题。例如：

```text
基线：全图使用 FP16，某层输出出现 inf/NaN
实验：只把这一个可疑层改成 BF16 或 FP32
其余输入、权重、模型、batch、随机种子、其他层都不变
观察：该层及下游的 inf/NaN 是否消失，任务指标是否恢复
```

若异常消失，说明该层的精度或范围策略是强相关根因；若不消失，再回到 First Bad Tensor 附近检查其他算子或上游输入。


## 九、通过标准

- 能独立区分 overflow、underflow、`inf`、`NaN` 与 subnormal；
- 能解释 FP16/BF16 在范围与精度上的取舍；
- 能用 First Bad Tensor 与单变量实验写出最小数值 Debug 方案。

完成本课练习后，将先进行章节评估；未达标时只在本文追加夯实内容。

---

## 十、根据当前作答调整的学习安排

### 当前判断

| 能力 | 当前表现 | 安排 |
|---|---|---|
| bias 与指数还原 | 能独立完成 `E=e-bias` | 后续间隔复习即可 |
| overflow / underflow | 已能区分“大到无法表示”和“小到无法表示” | 通过范围题巩固 |
| FP16/BF16 范围 | 已理解 BF16 指数位更多、范围更大 | 保留公式推导，不要求死记所有数值 |
| 特殊值编码 | 将 `指数全 1` 误解为普通整数/小数 | 优先夯实 |
| 数值 Debug | 知道找 NaN，但尚未掌握 First Bad Tensor 与单变量实验 | 优先夯实 |

### 本节后续顺序

1. **特殊值速记**：只区分正常数、0/subnormal、`inf`、`NaN`；
2. **范围判断**：用 FP16 上限 `65,504` 判断是否有 overflow 风险；
3. **Debug 最小闭环**：找 First Bad Tensor，再做一次只改一个 dtype 的实验；
4. **章节评估**：以上 3 类各 1 题。若 3 题中至少 2 题能独立答对，且能说出单变量实验的控制原则，则结束 L0.3；否则继续在本文夯实，不进入 L3。

### 夯实题 C1｜特殊值速记

以下 FP16 位模式各表示什么？不需要计算十进制值。

```text
A. 0 | 11111 | 0000000000
B. 1 | 11111 | 0000000000
C. 0 | 11111 | 0000000001
```

**我的回答：**

未填写。

**批改与答疑：** 指数位 `11111`（全为 1）是 FP16 的特殊编码；尾数栏不再表示普通小数部分。

```text
A. 0 | 11111 | 0000000000 → +inf
B. 1 | 11111 | 0000000000 → -inf
C. 0 | 11111 | 0000000001 → NaN
```

规则只需记一条：**指数全 1 + 尾数全 0 是无穷大；指数全 1 + 尾数非 0 是 NaN。** 符号位只决定 `+inf` 或 `-inf`；NaN 不需要区分正负。

### 夯实题 C2｜范围判断

某层输出 `max_abs=72,000`。若该层执行 FP16，最需要担心什么？最小的 dtype 调整建议是什么？

**我的回答：**
答：overflow，转bf16

**批改：正确。**

```text
72,000 > 65,504
→ FP16 有 overflow 风险，可能出现 +inf
→ 最小调整：仅将这个可疑层改为 BF16
```

这里的“最小”是指不把整张图都改为 BF16；先只改出现问题的层，再比较 `inf_count/nan_count` 与任务指标。

### 夯实题 C3｜First Bad Tensor 与单变量实验

按执行顺序采集到：

```text
Tensor A：max_abs=2.1，inf_count=0，nan_count=0
Tensor B：max_abs=inf，inf_count=6，nan_count=0
Tensor C：max_abs=NaN，inf_count=6，nan_count=4
```

请回答：

1. First Bad Tensor 是哪一个？

2. 设计一个单变量实验来验证该处是否受 FP16 范围限制。

**我的回答：**
答：
1. tensor b
2. 转为bf16，再查一下数值

**批改：第 1 问正确；第 2 问方向正确，但需要写清单变量控制条件。**

```text
First Bad Tensor：Tensor B
原因：A 仍全部有限；B 已出现 inf；C 的 NaN 是下游传播结果。
```

一个合格的单变量实验可以写成：

```text
基线：保持模型、输入、权重、batch、随机种子和其他层全为原设置；B 在 FP16 下出现 inf。
实验：只将产生 Tensor B 的算子/层改为 BF16（或 FP32），其他条件完全不变。
观察：比较 B、C 的 max_abs、inf_count、nan_count，以及最终任务指标。
```

若 B/C 的异常消失，说明该层受 FP16 范围或精度策略影响；若未消失，继续检查 B 的上游或该层内部的敏感算子。

### 夯实复答 D1｜特殊值与 Debug

请用自己的话完成两句：

```text
1. FP16 指数全 1、尾数全 0 表示 ______；尾数非 0 表示 ______。
2. 单变量实验的关键是：只改变 ______，其他条件 ______。
```

**我的回答：**

1. 指数全 1、尾数全 0 是 `±inf`；尾数非 0 是 `NaN`。
2. 可能导致 bad tensor 的条件，其他条件不变。

**批改：两句核心意思正确。** 第 2 句准确表述为：每次只改变一个怀疑会导致 bad tensor 的因素，其他条件保持不变。例如，只调整产生 Tensor B 的层的计算精度，保持输入、权重和其他层设置不变。若异常消失，支持该精度设置与问题有关，但还需进一步区分范围超限、舍入或实现差异。

**本轮评估：** D1 已完成，特殊值与单变量实验这轮定向夯实结束。舍入误差和 subnormal 尚缺独立作答证据，章节最终评估应覆盖这两项，不能据 D1 推断整章已经掌握。

### 章节检查 E1｜舍入误差

某数没有超过 FP16 的有限数范围，但转换成 FP16 后与原值有微小差别。这一定是 overflow 吗？请说明原因。

**我的回答：**

不是，因为没有超过范围。

**批改：判断正确。** 已能排除上溢；误差来源补充如下：有限数范围以内也不是每个实数都能精确表示。FP16 尾数位有限，转换时可能需要舍入到邻近的可表示值，因此产生微小差别。范围内不代表一定能精确表示。

### 章节检查 E2｜subnormal

在支持 subnormal、未开启 FTZ 的条件下，FP16 的指数位全为 0、尾数非 0，表示的是 0、subnormal、inf 还是 NaN？它的绝对值与最小正常数 `2⁻¹⁴` 相比更大还是更小？

**我的回答：**

表示小数，比最小正常数更大；不知道什么是 subnormal。

**批改：需要修正。** 指数全为 0、尾数非 0 表示 **subnormal（非规格化数）**，并且它的绝对值比最小正常数 `2⁻¹⁴` 更小。

### 答疑：什么是 subnormal？

正常 FP16 数的有效数字固定从隐藏位 `1` 开始：

```text
normal：1.fraction × 2^E
```

当指数位全为 0 时，格式不再使用隐藏的 `1`，而改用：

```text
subnormal：0.fraction × 2⁻¹⁴
```

这让 FP16 能在 0 附近继续表示一小段更小的非零数，而不是从最小正常数 `2⁻¹⁴` 直接跳到 0。

```text
最小正常数：1.0000000000₂ × 2⁻¹⁴ = 2⁻¹⁴
最大的 subnormal：0.1111111111₂ × 2⁻¹⁴（略小于 2⁻¹⁴）
最小正 subnormal：0.0000000001₂ × 2⁻¹⁴ = 2⁻²⁴
```

可以把 subnormal 想成 0 附近的一段“过渡台阶”。它避免很小的数突然变成 0，但可用精度更低。有些 NPU 或编译设置会启用 FTZ（flush-to-zero），将这类极小数直接当作 0，以换取更简单或更快的计算。

**E2 正确结论：** 指数全 0、尾数非 0 → subnormal；其绝对值小于最小正常数 `2⁻¹⁴`。

**理解检查：** `2⁻²⁴` 比 `2⁻¹⁴` 更小。回答正确；指数越负，数值越接近 0。

---

## 十一、第 02 课最终评估

### 学习情况

| 能力 | 结果 |
|---|---|
| FP16 overflow 范围判断 | 能用 `65,504` 判断 FP16 风险，并提出将可疑层改为 BF16 |
| underflow 与舍入 | 能区分大数溢出和范围内的舍入误差 |
| `inf` / `NaN` | 经讲解后能正确复述指数全 1 的规则 |
| subnormal | 经讲解后能判断其位于 0 与最小正常数之间 |
| First Bad Tensor | 能识别第一个出现 `inf` 的 Tensor，而不是只看下游 NaN |
| 单变量实验 | 能说出只改可疑因素、其他条件不变的原则 |

### 结论

**通过本章，进入 L3.1。** 当前对特殊值与 subnormal 的理解仍以刚学后的短时记忆为主，因此第 03 课会保留两道 L0.3 间隔复习题；若下次无法独立回答，再回到本章追加夯实。

### 下一课

第 03 课学习对称 INT8 量化：为什么要量化、如何计算 scale、如何量化/反量化，以及量化误差从哪里来。
### 答疑：为什么要这样规定？

FP16 的指数栏共有 32 种编码。IEEE 754 风格的浮点格式没有把它们全部用于普通数，而是专门保留两端：

```text
指数全 0 → 0 与 subnormal（处理非常接近 0 的数）
指数 1～30 → 普通规格化有限数
指数全 1 → inf 与 NaN（处理溢出和非法计算）
```

当指数全为 1 时，普通的浮点数公式不再适用：

```text
尾数全 0 → +inf 或 -inf
尾数非 0 → NaN
```

符号位决定 `+inf` 或 `-inf`。`+inf` 常用来表示正向 overflow，例如把大于 FP16 最大有限值的正数转换为 FP16；`-inf` 对应负向 overflow。

`NaN` 表示“不是一个可比较、可参与普通数值运算的数”，常由 `0/0`、`inf-inf`、对负数做不允许的开方等非法或未定义计算产生。它通常会向下游传播，帮助 Debug 尽早暴露数值问题。

这样设计的好处是：有限数溢出时能有明确的 `inf` 信号；非法运算时能有明确的 `NaN` 信号，而不是悄悄伪装成一个看似正常的巨大数或 0。

> 备注：虽然 NaN 位模式也保留了一个符号位，但 NaN 没有“正/负数值”的通常含义。
