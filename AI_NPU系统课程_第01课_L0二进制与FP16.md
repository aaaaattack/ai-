# 第 01 课｜L0：二进制科学计数法与 FP16

## 一、学习目标

学完本课后，你应能：

1. 把简单十进制整数和小数转换为二进制；
2. 把二进制数写成规格化科学计数法；
3. 理解 FP16 的符号位、指数位、尾数位和 bias；
4. 手工编码、解码简单 FP16 数值；
5. 解释指数位和尾数位分别影响数值范围与精度；
6. 将浮点溢出、舍入误差与 NPU 精度问题联系起来。

---

## 二、先理解“位权”

十进制数 `352.4` 的每一位都有不同权重：

```text
352.4
= 3×10² + 5×10¹ + 2×10⁰ + 4×10⁻¹
```

二进制完全一样，只是底数从 10 变成 2：

```text
1101.101₂
= 1×2³ + 1×2² + 0×2¹ + 1×2⁰
  + 1×2⁻¹ + 0×2⁻² + 1×2⁻³
= 8 + 4 + 0 + 1 + 0.5 + 0 + 0.125
= 13.625₁₀
```

需要先记住常见位权：

| 二进制位置 | 位权 | 十进制值 |
|---|---:|---:|
| 小数点左第 1 位 | `2⁰` | 1 |
| 小数点左第 2 位 | `2¹` | 2 |
| 小数点左第 3 位 | `2²` | 4 |
| 小数点左第 4 位 | `2³` | 8 |
| 小数点右第 1 位 | `2⁻¹` | 0.5 |
| 小数点右第 2 位 | `2⁻²` | 0.25 |
| 小数点右第 3 位 | `2⁻³` | 0.125 |
| 小数点右第 4 位 | `2⁻⁴` | 0.0625 |

### 直观理解

十进制小数点右边依次是十分之一、百分之一；二进制小数点右边依次是二分之一、四分之一、八分之一。

---

## 三、十进制整数转二进制

以 `13` 为例，可以把它拆成 2 的幂：

```text
13 = 8 + 4 + 1
   = 1×2³ + 1×2² + 0×2¹ + 1×2⁰
   = 1101₂
```

也可以连续除以 2，记录余数，再从下往上读：

```text
13 ÷ 2 = 6 余 1
 6 ÷ 2 = 3 余 0
 3 ÷ 2 = 1 余 1
 1 ÷ 2 = 0 余 1

从下往上：1101₂
```

对便于手算的小数字，优先使用“拆成 2 的幂”，更直观。

---

## 四、十进制小数转二进制

### 方法一：直接使用位权

例如：

```text
0.625 = 0.5 + 0.125
      = 1×2⁻¹ + 0×2⁻² + 1×2⁻³
      = 0.101₂
```

### 方法二：连续乘以 2

每次取结果的整数部分：

```text
0.625 × 2 = 1.25  → 取 1，剩 0.25
0.25  × 2 = 0.5   → 取 0，剩 0.5
0.5   × 2 = 1.0   → 取 1，结束

所以 0.625 = 0.101₂
```

有些十进制小数无法用有限位二进制精确表示。例如十进制 `0.1` 在二进制中是无限循环小数，所以存入浮点数时必须舍入。这正是许多浮点误差的来源。

---

## 五、二进制科学计数法

十进制科学计数法要求小数点左边只有一个非零数字：

```text
6250 = 6.25 × 10³
```

二进制只有 0 和 1，所以规格化数的小数点左边必定是 `1`：

```text
1101.01₂ = 1.10101₂ × 2³
```

小数点向左移动 3 位，因此指数是 `+3`。

再看小于 1 的数：

```text
0.011₂ = 1.1₂ × 2⁻²
```

要把小数点向右移动 2 位才得到 `1.1`，因此指数是 `-2`。

### 必须掌握的判断

- 原数大于等于 2：通常需要把小数点左移，指数为正；
- 原数位于 1 到 2：不需要移动，指数为 0；
- 原数小于 1：需要把小数点右移，指数为负。

---

## 六、浮点数为什么要拆成三部分

规格化二进制浮点数可以写成：

```text
数值 = (-1)^S × 1.fraction × 2^E
```

- `S`：符号位；
- `fraction`：有效数字的小数部分，也叫尾数部分；
- `E`：真实指数，控制小数点移动位置。

FP16 的布局：

```text
| 1 bit 符号 | 5 bit 指数 | 10 bit 尾数 |
```

对普通规格化数，实际公式是：

```text
value = (-1)^S × (1 + fraction) × 2^(e - bias)
```

这里：

- `e` 是指数位里实际存储的无符号整数；
- FP16 的 `bias=15`；
- `E=e-bias` 才是真实指数。

---

## 七、bias 到底是什么

真实指数既可能为正，也可能为负，例如 `-14～+15`。但硬件希望指数位直接作为无符号二进制整数保存，所以先整体加一个偏移量：

```text
存储指数 e = 真实指数 E + bias
真实指数 E = 存储指数 e - bias
```

FP16 的 bias 是 15：

| 真实指数 E | 存储指数 e | 5 bit 编码 |
|---:|---:|---:|
| -3 | 12 | `01100` |
| -1 | 14 | `01110` |
| 0 | 15 | `01111` |
| 1 | 16 | `10000` |
| 2 | 17 | `10001` |

### 为什么不直接给指数加一个符号位

使用 bias 后：

- 指数可以按无符号整数存储和比较；
- 数值越大的正常指数通常对应越大的编码；
- 还能把全 0 和全 1 的指数编码留给特殊值。

你可以把 bias 理解成“指数坐标系的原点平移”：真实指数 0 在存储时不写 0，而写成 15。

---

## 八、带做例题 1：把 `6.5` 编码成 FP16

### 第一步：转换成二进制

```text
6 = 110₂
0.5 = 0.1₂
6.5 = 110.1₂
```

### 第二步：规格化

```text
110.1₂ = 1.101₂ × 2²
```

所以真实指数 `E=2`。

### 第三步：确定三个字段

正数：

```text
S = 0
```

指数：

```text
e = E + bias = 2 + 15 = 17 = 10001₂
```

尾数只保存 `1.101` 中小数点后的部分，开头的 `1` 是隐藏位：

```text
fraction = 1010000000
```

最终：

```text
0 | 10001 | 1010000000
```

---

## 九、带做例题 2：解码一个简化 FP16

给定位模式：

```text
0 | 10000 | 0100000000
```

符号位为 0，所以是正数。

指数：

```text
e = 10000₂ = 16
E = 16 - 15 = 1
```

尾数 `0100000000` 表示：

```text
fraction = 0×2⁻¹ + 1×2⁻² = 0.25
有效数字 = 1 + 0.25 = 1.25
```

最终数值：

```text
1.25 × 2¹ = 2.5
```

---

## 十、指数位和尾数位分别决定什么

可以把浮点数想象成一把可以伸缩的尺子：

- 指数位决定尺子能够覆盖多远，即**数值范围**；
- 尾数位决定尺子的刻度有多细，即**精度**。

因此：

- 指数位太少：大数容易 overflow，小数容易 underflow；
- 尾数位太少：相邻可表示数间隔更大，舍入误差更明显。

FP16 与 BF16 都是 16 bit：

| 格式 | 指数位 | 尾数位 | 主要特点 |
|---|---:|---:|---|
| FP16 | 5 | 10 | 精度相对更细，但范围较小 |
| BF16 | 8 | 7 | 范围接近 FP32，但精度更粗 |

这也是为什么大模型 activation 范围较大时，BF16 往往比 FP16 更不容易 overflow；但在需要细小数值差异时，FP16 的尾数精度更好。

---

## 十一、这与 NPU Debug 有什么关系

当模型从 FP32 转为 FP16 后出现精度异常，你需要检查：

1. 中间 Tensor 是否出现 `inf` 或 `NaN`；
2. 最大绝对值是否超过 FP16 可表示范围；
3. 很小的数是否 underflow 成 0；
4. 大数附近的细小差异是否因尾数不足被舍入掉；
5. Softmax、Exp、Norm、除法等数值敏感算子是否需要更高精度；
6. 将可疑层恢复到 FP32/BF16 后，误差是否消失。

这就是“浮点位结构”如何转化为实际精度定位手段。

---

## 十二、本课小结

记住以下主线：

```text
十进制数
→ 二进制位权
→ 1.fraction × 2^E
→ 符号位 S
→ 存储指数 e=E+bias
→ 尾数保存 fraction
```

最关键的三个公式：

```text
e = E + bias
E = e - bias
value = (-1)^S × (1 + fraction) × 2^E
```

---

## 十三、课后习题（10 题）

### 第 1 题｜位权理解

二进制数 `1011.01₂` 中，从最高位到最低位分别对应哪些 2 的幂？将它转换为十进制。

**我的回答：**

从高到低，各位分别对应 `2³、2²、2¹、2⁰、2⁻¹、2⁻²`。

```text
1011.01₂
= 1×2³ + 0×2² + 1×2¹ + 1×2⁰ + 0×2⁻¹ + 1×2⁻²
= 2³ + 2¹ + 2⁰ + 2⁻²
```

**批改：正确。** 位权和非零项展开均正确；补全最后一步：

```text
8 + 2 + 1 + 0.25 = 11.25₁₀
```


### 第 2 题｜整数转二进制

将十进制整数 `13` 转换为二进制。请写出拆分为 2 的幂的过程。

**我的回答：**

`13=8+4+1=2³+2²+2⁰`，最初写成了 `111₂`。

**批改：部分正确。** 数值拆分完全正确，但二进制是按连续位权 `2³、2²、2¹、2⁰` 排列的；即使 `2¹` 的系数为 0，也不能省略该位：

```text
13
= 8 + 4 + 0 + 1
= 1×2³ + 1×2² + 0×2¹ + 1×2⁰
= 1101₂
```

`111₂` 实际表示 `2²+2¹+2⁰=7`。


### 第 3 题｜小数转二进制

将十进制小数 `0.625` 转换为二进制。请分别用位权法或连续乘 2 法说明过程。

**我的回答：**

`0.625=1×2⁻¹+0×2⁻²+1×2⁻³`。

**批改：正确。** 补全十进制位权值和最终二进制结果：

```text
0.625
= 0.5 + 0 + 0.125
= 1×2⁻¹ + 0×2⁻² + 1×2⁻³
= 0.101₂
```


### 第 4 题｜二进制科学计数法

将十进制数 `10.625` 转换为二进制，并写成 `1.fraction × 2^E`，指出真实指数 `E`。

**我的回答：**

二进制为 `1010.101₂`，最初不确定指数。

**批改：二进制转换正确；规格化步骤经提示后补全。** 把小数点向左移动 3 位，使小数点左边只保留一个 `1`：

```text
10.625₁₀ = 1010.101₂
1010.101₂ = 1.010101₂ × 2³
```

因此真实指数 `E=3`。反向检查时，`1.010101₂` 的小数点向右移动 3 位，正好恢复为 `1010.101₂`。


### 第 5 题｜bias 计算

某 FP16 规格化数的真实指数 `E=-4`。请计算存储指数 `e` 的十进制值和 5 bit 二进制编码。

**我的回答：**

`e=-4+15=11`，最初写成二进制 `1011₂`，但不理解 bias 的含义。

**批改：计算正确；5 bit 格式需要补前导 0。**

```text
e = E + bias
  = -4 + 15
  = 11
  = 01011₂（5 bit）
```

**bias 补充讲解：** 指数可能为负数，但 FP16 的 5 个指数位只直接存储 `0～31` 的无符号整数。因此编码时把真实指数整体向右平移 15：

```text
编码：e = E + 15
解码：E = e - 15
```

例如，真实指数 `-4` 平移后存为 `11`；读取时再用 `11-15` 恢复为 `-4`。bias 只是指数的编码偏移量，不是数值本身额外乘了 15。

**理解检查：** 若存储指数 `e=18`，则真实指数为 `E=18-15=3`，写成 5 bit 是 `00011₂`。回答正确。


### 第 6 题｜FP16 解码

解码以下 FP16 位模式：

```text
0 | 10000 | 0100000000
```

请写出符号、存储指数、真实指数、有效数字和最终十进制值。

**我的回答：**

符号为正，`e=16`，`E=1`；最初将最终乘数误写为 `2⁸`。

**批改：前 3 步正确；尾数与最终计算补全如下。**

```text
S = 0，所以数值为正
e = 10000₂ = 16
E = e - bias = 16 - 15 = 1

fraction = 0×2⁻¹ + 1×2⁻² = 0.25
有效数字 = 1 + fraction = 1.25

value = (+1) × 1.25 × 2¹ = 2.5
```

注意：指数位模式 `10000₂` 的十进制值是 16；经 `bias=15` 还原后，真实指数才是 1。指数位的二进制形状不是最终要使用的 `2` 的幂。


### 第 7 题｜FP16 编码

将十进制数 `-0.75` 编码为 FP16，写出完整过程和最终的 16 bit 位模式。

**我的回答：**

不会。

**讲解与标准答案：** FP16 编码按“符号 → 二进制 → 规格化 → 指数 → 尾数”完成。

```text
-0.75₁₀ = -0.11₂
        = -1.1₂ × 2⁻¹
```

因此：

```text
S = 1                         （负数）
E = -1
e = E + bias = -1 + 15 = 14
e = 01110₂                    （5 bit）
```

规格化后的有效数字是 `1.1₂`。前面的 `1` 是隐藏位，只保存小数点后的 `1`，再在右侧补 0 到 10 bit：

```text
fraction = 1000000000
```

最终位模式为：

```text
1 | 01110 | 1000000000
```

反向检查：`E=14-15=-1`，有效数字为 `1.5`，所以数值为 `-1.5×2⁻¹=-0.75`。


### 第 8 题｜范围与精度

为什么 BF16 比 FP16 更不容易 overflow，但通常有更大的舍入误差？请分别从指数位和尾数位解释。

**我的回答：**

FP16 的指数位有 5 bit、尾数位有 10 bit。BF16 不容易溢出，因为数值范围更大；误差更大，因为尾数位更少、可表示的精度更低。

**批改：核心因果正确。** 补全 BF16 的位数并将原因与字段对应起来：

```text
FP16：1 bit 符号 + 5 bit 指数 + 10 bit 尾数
BF16：1 bit 符号 + 8 bit 指数 +  7 bit 尾数
```

- BF16 的指数位更多（8 bit），可表示的真实指数范围更大，因此大数更不容易 overflow、小数更不容易 underflow。
- BF16 的尾数位更少（7 bit），相邻可表示数的间隔更大；将数值存入 BF16 时更容易发生舍入，因此精度通常低于 FP16。

一句话：**指数位管范围，尾数位管精度。**


### 第 9 题｜精度 Debug

某模型从 FP32 切换到 FP16 后，某层输出的最大绝对值从 `50,000` 增长到 `80,000`，随后出现 `inf` 和 `NaN`。请提出两个根因假设，并为每个假设设计一个单变量实验。

**我的回答：**

请求讲解。

**参考解：** 看到 `80,000` 后出现 `inf/NaN`，首先要注意 FP16 的最大有限值约为 `65,504`；因此不能直接归因于硬件，而应先定位“是范围真的超限，还是某个敏感计算的精度策略有问题”。

**根因假设 1：该层的真实输出范围超过 FP16 上限。**

- 可能原因：上游输入异常放大、权重尺度过大、缺少归一化，或残差相加后使输出从 `50,000` 增至 `80,000`。
- 单变量实验：保持模型、输入和其他层的 dtype 不变，只将该层及其输出保留为 BF16 或 FP32；分别记录该层的 `max_abs`、`inf/NaN` 比例和最终任务指标。若异常消失，而 FP32/BF16 下仍观测到接近 `80,000` 的有限值，则证实核心问题是 FP16 范围不足。

**根因假设 2：敏感子算子或累加在 FP16 中发生数值不稳定。**

- 可能原因：该层内部的 `exp`、除法、归一化、归约累加或 softmax 使用了 FP16 的输入/累加，先产生 `inf`，再向下游传播为 `NaN`。
- 单变量实验：仅把可疑子算子的计算和累加切换为 FP32（例如用 FP32 执行 softmax、归一化或 reduction），其余图、权重和输入保持 FP16；逐层 dump `max_abs`、`inf/NaN` 比例并比较结果。若异常消失，说明问题来自该算子的精度策略，而非整体硬件错误。

**证据闭环：** 先找到第一个出现 `inf/NaN` 的 Tensor，再用上述每次只改一个变量的实验区分“真实范围超限”和“敏感算子计算不稳定”。


### 第 10 题｜客户场景

客户说：“FP16 和 BF16 都是 16 bit，所以精度和范围应该完全一样。”请用不超过 150 字纠正这个说法，并说明在 NPU 模型部署中如何选择。

**我的回答：**

请求讲解。

**参考答案（124 字）：** 这句话不对。FP16 有 5 bit 指数和 10 bit 尾数，尾数更多、精度较好，但数值范围较小；BF16 有 8 bit 指数和 7 bit 尾数，范围接近 FP32、更不易溢出，但舍入误差更大。部署时，激活范围大或容易溢出的模型优先 BF16；需要更细数值精度时可选 FP16，敏感层必要时保留 FP32。


---

## 完成标准

- 第 1～7 题能够独立计算；
- 第 8 题能说清“指数位控制范围、尾数位控制精度”；
- 第 9～10 题能够把数值表示连接到 NPU 精度定位。

完成后保存本文并告诉我“第 1 课完成”。我会逐题批改，然后判断进入 L0.3，还是先补练二进制和 bias。

---

## 十四、本章学习评估与夯实判定

### 当前掌握情况

| 能力 | 观察结果 | 判定 |
|---|---|---|
| 二进制位权与小数 | 能正确展开 `1011.01₂`、`0.625` | 初步掌握 |
| 整数转二进制 | 能拆成 2 的幂，但曾漏写中间的 `0` 位 | 需要巩固 |
| 规格化与真实指数 | 能写出二进制，但初始不能独立确定小数点移动和 `E` | 需要巩固 |
| bias | 能算出 `e=E+15`，经讲解后能完成反向还原 | 正在形成 |
| FP16 解码 | 能识别符号、存储指数和真实指数，但尾数与最终公式需提示 | 需要巩固 |
| FP16 编码 | 尚不能独立将数值写成 16 bit 位模式 | 需要重点夯实 |
| FP16/BF16 对比 | 已理解“指数管范围、尾数管精度” | 初步掌握 |
| 精度 Debug 与部署选型 | 当前以阅读参考答案为主，尚未独立作答 | 待后续回练 |

### 本章结论

**暂不进入 L0.3。** 当前独立完成能力约为 45%～55%，尚未达到“核心题正确率约 80%，并能解释计算过程”的章节门槛。优先夯实 FP16 的“规格化 → bias → 尾数 → 编码/解码”主线；否则后续 overflow、underflow 和量化学习会反复卡住。

### 夯实训练 A：FP16 编码与解码核心链路

本轮只练以下固定步骤：

```text
十进制数
→ 二进制
→ 规格化 1.fraction × 2^E
→ e = E + 15
→ 尾数补足 10 bit
→ 1 | 5 | 10 的 FP16 位模式
```

完成标准：6 题中至少答对 5 题，且第 4 或第 5 题能够独立写出完整 FP16 位模式；达到后再进行 L0.3。

### 夯实题 A1｜不跳过中间位

将 `19₁₀` 写成二进制。请按 `2⁴、2³、2²、2¹、2⁰` 的顺序写出每一位。

**我的回答：**

`1、0、0、1、1`。

**批改：正确。**

```text
19 = 1×2⁴ + 0×2³ + 0×2² + 1×2¹ + 1×2⁰
   = 16 + 2 + 1
   = 10011₂
```

### 夯实题 A2｜规格化

将 `0.75₁₀` 写成二进制科学计数法 `1.fraction × 2^E`，指出 `E`。

**我的回答：**

`0.75₁₀=0.11₂=1.1₂×2⁻¹`，`E=-1`。

**批改：正确。** 小于 1 的数需要将小数点向右移动到第一个 `1` 后，因此真实指数为负数。

### 夯实题 A3｜bias 正反算

FP16 中，真实指数 `E=-2` 时，存储指数 `e` 的十进制值和 5 bit 编码分别是什么？

**我的回答：**

`e=13`，二进制最初写成 `00111₂`。

**批改：部分正确。**

```text
e = E + 15 = -2 + 15 = 13
13 = 8 + 4 + 1 = 2³ + 2² + 2⁰
13₁₀ = 1101₂ = 01101₂（5 bit）
```

`00111₂` 等于 `4+2+1=7`。将十进制转二进制时，先拆成 2 的幂，再按连续位权填 `0/1`，能避免这种错位。

### 夯实题 A4｜解码

解码位模式 `0 | 01110 | 1000000000`，写出 `S`、`e`、`E`、有效数字和最终十进制值。

**我的回答：**

`S=0`，`e=14`，`E=-1`，最终值为 `1.5×2⁻¹=0.75`；最初将 `(-1)^S` 当作有效数字的一部分。

**批改：除有效数字的定义外，计算正确。**

```text
S = 0，因此符号因子 (-1)^S = +1
e = 01110₂ = 14
E = 14 - 15 = -1

fraction = 1×2⁻¹ = 0.5
有效数字 = 1 + fraction = 1.5

value = (+1) × 1.5 × 2⁻¹ = 0.75
```

请区分三个部分：`(-1)^S` 决定正负；`1+fraction` 是有效数字；`2^E` 决定缩放倍率。

### 夯实题 A5｜编码

将 `+2.5₁₀` 编码为 FP16，写出二进制、规格化形式、`S`、`e`、10 bit 尾数和最终位模式。

**我的回答：**

`2.5₁₀=10.1₂=1.01₂×2¹`，`S=0`，`e=16`，`fraction=0100000000`。

**批改：正确。** 补全存储指数的 5 bit 表示和最终位模式：

```text
E = 1
e = E + 15 = 16 = 10000₂
fraction = 0100000000

最终：0 | 10000 | 0100000000
```

反向检查：有效数字 `1.01₂=1.25`，真实指数 `E=1`，所以 `1.25×2=2.5`。

### 夯实题 A6｜格式选择

某层 activation 最大绝对值可能达到 `90,000`。在 FP16 与 BF16 中优先选择哪一种？请用“指数位、数值范围、溢出风险”说明原因。

**我的回答：**

选择 BF16，因为指数位更多、表示的数值范围更大，溢出风险更小；最初将数值范围表述为 `0～2⁸-1`。

**批改：选型与核心理由正确。**

```text
选择：BF16
指数位：BF16 有 8 bit，FP16 只有 5 bit
数值范围：更多指数位使 BF16 的范围接近 FP32，能覆盖远大于 90,000 的有限数
溢出风险：90,000 已超过 FP16 最大有限值约 65,504；BF16 溢出风险显著更小
```

`0～255` 是 8 bit 指数栏可存的**编码整数**范围；它不是 BF16 最终数值的范围。最终数值还要通过尾数和 `E=e-bias` 一起计算。

### 夯实训练 A 评估

你已独立完成 A1、A2、A5，A6 的选型判断正确；A3 的二进制编码、A4 的字段职责仍需提示。当前不直接进入 L0.3，先进行短复测，确认这些关键点已经稳定。

### 夯实复测 B1｜指数编码

FP16 中，真实指数 `E=-2`。请写出 `e` 的十进制值和 **5 bit** 二进制值。

**我的回答：**

`e=13`，`01101₂`（上一条输入中的 `01011` 为笔误）。

**批改：正确。**

```text
e = -2 + 15 = 13
13₁₀ = 01101₂（5 bit）
```

### 夯实复测 B2｜字段分工

解码 `1 | 10000 | 0100000000`。请分别写出符号因子、有效数字、真实指数与最终十进制值。

**我的回答：**

符号因子 `-1`，有效数字 `1.5`，真实指数 `1`，最终十进制值 `-3`。

**批改：正确。**

```text
(-1)^S = (-1)^1 = -1
1 + fraction = 1 + 0.5 = 1.5
E = 16 - 15 = 1
value = (-1) × 1.5 × 2¹ = -3
```

### 夯实复测 B3｜范围的含义

为什么“BF16 的指数位有 8 bit”不等于“BF16 的数值范围只是 0～255”？请用一句话说明指数编码整数、bias 与最终浮点数值之间的关系。

**我的回答：**

请求重新讲解 bias。

**从零讲解 bias：** 浮点数的真实指数 `E` 既可能为正，也可能为负，例如 `2³`、`2⁰`、`2⁻⁴`。但指数位本身没有正负号，只能保存非负整数。于是格式规定先给真实指数加一个固定偏移量，这个偏移量就叫 **bias**。

```text
存储时：e = E + bias
读取时：E = e - bias
```

把 bias 想成“楼层编号的整体平移”：真实的 0 楼不写成 0，而写成某个中间编号；低于 0 楼的负楼层也能用非负编号保存。

```text
FP16：bias = 15
真实指数 E = -2  → 存储 e = -2 + 15 = 13
存储 e = 16      → 真实 E = 16 - 15 = 1
```

```text
BF16：bias = 127
指数栏 8 bit 可保存的 e 是 0～255，这只是“编号”
例如 e = 129 → E = 129 - 127 = 2
若尾数为 0、符号为正，最终值是 1.0×2²=4
```

因此，`0～255` 不是 BF16 最终浮点数的范围；它只是存储指数 `e` 的编码范围。最终数值还要把 `e` 用 bias 还原为 `E`，再与有效数字一起代入：

```text
value = (-1)^S × (1 + fraction) × 2^(e - bias)
```

**B3 结论：** BF16 的 8 bit 指数栏保存的是编码整数 `e`；减去 `bias=127` 才得到真正参与 `2^E` 计算的指数，最终浮点数值还取决于尾数，因此范围绝不是简单的 `0～255`。

**理解检查：** FP16 中 `e=14` 时，`E=14-15=-1`。回答正确。

---

## 十五、第 01 课最终评估

### 结论：达到进入 L0.3 的门槛

经过夯实训练与复测，已经能够：

- 按连续位权书写二进制，不再遗漏中间的 `0`；
- 将十进制小数写成规格化形式，并判断真实指数的正负；
- 使用 `e=E+bias` 与 `E=e-bias` 完成 FP16 指数编码/解码；
- 独立完成 `+2.5` 的 FP16 编码和 `-3` 的 FP16 解码；
- 解释 FP16/BF16 的指数位、数值范围与溢出风险之间的关系。

### 仍需保持的复习点

- 不把存储指数 `e` 与真实指数 `E` 混为一谈；
- 不把符号因子、有效数字和 `2^E` 混为一项；
- 需要继续通过实际 overflow/underflow 场景巩固 bias 的意义。

### 下一步

进入 **第 02 课｜L0.3：overflow、underflow、舍入误差与特殊值**。第 02 课会在开头和结尾各插入 1 道 bias/FP16 编码回顾题，作为间隔复习。
