# 第 04 课｜L4.1：MAC、Ops、TOPS 与理论峰值算力

## 一、学习目标

完成本课后，你应能：

1. 区分 MAC、Ops、GMAC/s、TOPS；
2. 根据核心数、每周期 MAC 数和时钟频率计算理论峰值；
3. 识别厂商“1 MAC = 2 Ops”的统计口径；
4. 用实测吞吐计算算力利用率；
5. 理解峰值算力不等于端到端实际性能。

---

## 二、先统一单位

### 1. 什么是 MAC

MAC 是 **Multiply-Accumulate**，即一次乘法加一次累加：

```text
acc = acc + a × b
```

矩阵乘法、卷积和全连接层的大量核心工作都是 MAC。

### 2. MAC 与 Ops

很多芯片厂商按以下口径统计：

```text
1 MAC = 1 次乘法 + 1 次加法 = 2 Ops
```

因此同一硬件的数字可能写成两种形式：

```text
8 TMAC/s = 16 TOPS（若 1 MAC 按 2 Ops 统计）
```

看性能资料时必须先确认口径，否则数字会相差 2 倍。

### 3. 常用数量级

| 单位 | 含义 |
|---|---|
| MHz | 每秒百万个时钟周期 |
| GHz | 每秒十亿个时钟周期 |
| GMAC/s | 每秒十亿次 MAC |
| TMAC/s | 每秒万亿次 MAC |
| TOPS | 每秒万亿次运算 |

```text
1 GHz = 10⁹ cycle/s
1 TOPS = 10¹² Ops/s
```

---

## 三、峰值算力公式

若 NPU 有：

- `C` 个计算核心；
- 每个核心每周期完成 `M` 个 MAC；
- 主频为 `f` Hz；

则：

```text
MAC/s = C × M × f
Ops/s = MAC/s × 2       （仅当厂商采用 1 MAC = 2 Ops）
TOPS = Ops/s / 10¹²
```

### 带做例题

某 NPU 有 16 个计算核心，每个核心每周期完成 512 个 INT8 MAC，主频 1 GHz。

```text
MAC/s = 16 × 512 × 1×10⁹
      = 8.192×10¹² MAC/s
      = 8.192 TMAC/s

Ops/s = 8.192×10¹² × 2
      = 16.384×10¹² Ops/s
      = 16.384 TOPS
```

若实测 INT8 吞吐是 8 TOPS：

```text
利用率 = 实测 TOPS / 理论 TOPS
       = 8 / 16.384
       ≈ 48.8%
```

---

## 四、为什么实测通常低于峰值

理论峰值假设所有计算核心每个周期都在执行目标精度的有效 MAC。真实模型经常做不到，因为还会受到以下因素限制：

- 内存带宽不足，计算核心在等数据；
- 小算子、分支、同步和 launch 开销；
- layout conversion、reshape 或 CPU fallback；
- 非目标精度算子，或无法融合的图结构；
- batch 太小、shape 不规则，导致阵列利用率低。

因此 TOPS 是硬件上限，不是任何模型都能达到的保证值。

---

## 五、精度口径必须匹配

芯片常分别给出 INT8、FP16、BF16 或稀疏模式的峰值。比较时至少确认：

```text
精度是否一致
MAC/OPS 统计口径是否一致
是否使用稀疏、结构化稀疏或特殊加速模式
频率与功耗模式是否一致
```

例如 INT8 的 16 TOPS 不能直接当作 FP16 的 16 TOPS，也不能直接推断某个端到端模型延迟。

---

## 六、本课练习

### 第 1 题｜口径换算

若芯片吞吐为 `6 TMAC/s`，厂商采用 `1 MAC = 2 Ops`，它应标为多少 TOPS？

**我的回答：**


### 第 2 题｜峰值计算

某加速器有 8 个核心，每个核心每周期完成 256 个 INT8 MAC，频率为 1.2 GHz。请计算：

1. 每秒 MAC 数；
2. TMAC/s；
3. 若 1 MAC = 2 Ops，对应 TOPS。

**我的回答：**


### 第 3 题｜利用率

第 2 题加速器实测为 3 TOPS。按相同口径计算利用率。

**我的回答：**


### 第 4 题｜工程判断

某芯片宣传 INT8 32 TOPS，但一个模型实测只有 5 TOPS。请列出两个不能直接认定“硬件性能虚标”的原因。

**我的回答：**


## 七、通过标准

- 能独立完成 MAC、Ops 和 TOPS 换算；
- 能正确计算峰值与利用率；
- 能解释理论峰值和实际性能之间的差异。

完成后先评估；未达标时只在本文追加夯实内容。下一节学习内存带宽、Arithmetic Intensity 与 Roofline。

---

## 八、自学补充：理解峰值算力时还应知道什么

### 1. 计算阵列如何提高 MAC 吞吐

NPU、GPU Tensor Core 或 AI ASIC 通常不是逐个执行标量乘法，而是同时处理成批的矩阵元素。常见实现包括 SIMD/SIMT 向量单元、矩阵乘法单元和 systolic array（脉动阵列）。它们的共同目标是在一个周期内并行完成大量 MAC。

峰值 TOPS 的本质就是：并行单元数量 × 每个单元每周期 MAC 数 × 频率。实际模型必须有足够规则的矩阵/卷积形状，才能让这些单元保持忙碌。

### 2. batch、shape 与利用率

小 batch、小矩阵、动态 shape 或大量细碎算子，常使矩阵单元未被填满。即使峰值算力很高，利用率也可能很低。

```text
大而规则的 GEMM / Conv → 通常更接近峰值
小而零碎的算子序列 → 常受调度、同步、访存影响
```

### 3. 频率、功耗与温度

峰值公式中的频率并不总是固定。不同功耗模式、温度限制、DVFS（动态调压调频）和多租户竞争都可能降低实际频率。因此性能测试应记录精度、频率/功耗模式、batch、shape 和测量窗口。

### 4. 稀疏峰值

部分芯片会给出“结构化稀疏”下的更高 TOPS。只有模型权重或激活满足指定稀疏模式、编译器也启用对应路径时，才可能接近该数字。与普通 dense INT8 TOPS 比较时必须分开记录。

### 5. 读 profiler 时的常用字段

| 字段 | 含义 |
|---|---|
| latency | 单次执行耗时 |
| throughput | 单位时间完成的任务数或 token 数 |
| achieved TOPS | 实测计算吞吐 |
| utilization | 实测吞吐 / 理论峰值 |
| memory bandwidth | 实测数据搬运吞吐 |
| occupancy / array utilization | 计算阵列的忙碌程度 |

不要只看一个 TOPS 数字。端到端性能还要结合 CPU 预处理、H2D/D2H、图执行、fallback 与后处理。

### 6. 自学结论

峰值算力是硬件上限，描述“最多能做多少计算”；它本身不说明一个模型为何慢。下一课通过带宽和 Roofline 补齐这个判断。
